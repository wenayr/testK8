{
  "name": "mini-task-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "start": "pnpm --parallel -r start"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
